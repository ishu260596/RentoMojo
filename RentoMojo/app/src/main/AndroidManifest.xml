<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.rentomojo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RentoMojo">
        <activity android:name=".activities.OfferPromotionsActivity">

        </activity>
        <activity android:name=".packageactivity.PackageActivity" />
        <activity android:name=".activities.GetStartedActivity" >

        </activity>
        <activity android:name=".activities.WelcomeActivity" />
        <activity android:name=".activities.SelectCityActivity" />
        <activity android:name=".activities.LoginAcitvity"/>
        <activity android:name=".activities.HomeActivity">

        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>